#include <bits/stdc++.h>

using namespace std;

int N, M, C, A, U, V, W;

int main() {
	freopen(".in", "r", stdin);
	freopen(".out", "w", stdout);
	scanf("%d %d %d", &N, &M, &C);
	while (N --) scanf("%d", &A);
	while (M --) scanf("%d %d %d", &U, &V, &W);
	printf("-1");
	return 0;
}
